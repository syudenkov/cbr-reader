version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: cbr-reader-postgres
    environment:
      POSTGRES_DB: cbr_reader
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dev -d cbr_reader"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis - Commented out for now (using in-memory cache initially)
  # redis:
  #   image: redis:7-alpine
  #   container_name: cbr-reader-redis
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis-data:/data
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 10s
  #     timeout: 3s
  #     retries: 5

volumes:
  postgres-data:
    driver: local
  # redis-data:
  #   driver: local
